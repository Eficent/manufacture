<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.rule" id="repair_team_rule_rule">
        <field name="name">repair_team</field>
        <field name="model_id" search="[('model','=','repair.order')]"
               model="ir.model"/>
        <field name="global" eval="True"/>
        <field name="domain_force">['|',('team_id','=',False),('team_id','in',user.repair_team_ids.ids)]</field>
    </record>

</odoo>
